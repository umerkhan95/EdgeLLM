version: '3.8'

services:
  mojo-dev:
    build:
      context: .
      dockerfile: Dockerfile.mojo
    volumes:
      - .:/workspace
    stdin_open: true
    tty: true
    command: bash -c "pixi shell"
